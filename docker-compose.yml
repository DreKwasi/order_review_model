version: '3.9'

services:
    app:
        build: .
        image: time_series_app
        container_name: time_series
        volumes:
            - .:/time_series
        ports:
            - 8501:8501
